apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: band-magic
    newName: band-magic
    newTag: latest

patches:
  - path: service-patch.yaml
