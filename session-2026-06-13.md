# Session — 2026-06-13

## Infrastructure / Config

- Fixed Firestore DB name: hardcoded default changed from `(default)` to `rollingblackoutapp-fsdb` in `firestore.ts`
- Confirmed GCP project ID: `rollingblackoutapp`
- Created `CLAUDE.md` with architecture overview, env vars, data flow, and stage color conventions
- Updated `CLAUDE.md` with DB correction, DocLink pattern, and stage color documentation

## Types (`src/types/index.ts`)

- Added `TrackStage`, `TrackStatus`, `NextTrackAction`, `Milestone` — full domain lifecycle types
- Added `DocLinkType`, `DocLink` interface (`{ url, title, type }`)
- Added `docLinks?: DocLink[]` to embedded `Track` interface
- Removed draft type definitions from `admin/page.tsx` (moved here)

## Brand Colors & Design Tokens (`src/app/globals.css`)

- Added `@font-face` for `AgincourtStd` from `/public/fonts/`
- Added `@layer base :root` design tokens:
  - Brand primitives: `--rb-yellow`, `--rb-purple`, `--rb-red`, `--rb-blue`
  - Feature title tokens: `--feature-title-typeface-style-stack`, `--feature-title-typeface-size-number` (36px), etc.
- Added `@theme inline` entries:
  - `--font-agincourt` references `--feature-title-typeface-style-stack`
  - `--text-feature-title` references `--feature-title-typeface-size-number`
  - Four full Tailwind color scales: `rbyellow-100–950`, `rbpurple-100–900`, `rbred-100–900`, `rbblue-100–900`
  - `rbyellow-500` anchored to `oklch(0.87 0.18 100.2)` — the existing `a/label/input` color
- Added `@layer components`:
  - Stage badge classes: `.stage-writing/tracking/mixing/mastering/unknown` + `-bg` variants
  - `.text-gradient-brand` — `bg-gradient-to-r from-rbblue-500 via-rbyellow-700 to-rbpurple-700` with clip-text

## Shared Utilities

- Created `src/lib/stage.ts` — `stageClass(stage)` and `stageBgClass(stage)` helpers
  - Replaces three duplicated `STAGE_COLORS` Record maps across components

## Drive Doc Links

- Added Drive Docs section to both admin forms (new + edit): type selector, label, URL input, add/remove per track
- Added Drive Docs display section on release detail page (below Notes), renders as clickable links
- Data stored as `docLinks[]` on each embedded track, saved through existing PUT/POST release endpoints

## Element Colors Pattern

Applied `/** element colors */` const block to all pages — colors keyed by component/element path so names describe what they control (e.g. `colors.docLinks.linkText`, `colors.noteThread.author`). Designed to be JSON-config-ready.

- `src/app/release/[id]/page.tsx`
- `src/app/page.tsx`
- `src/app/admin/page.tsx`
- `src/app/admin/[id]/page.tsx`
- `src/app/browse/page.tsx`

## Next.js `<Link>` Migration

Replaced `<a href>` with `<Link href>` from `next/link` for all internal page navigation across:
- `layout.tsx` (nav + brand logo)
- All page files

External links (`target="_blank"` Drive docs, GCS audio API route) kept as `<a>`.

## Layout

- Background image: changed `absolute` → `fixed` on container div — stops background jumping between page navigations. Added `brightness-[0.15]` (was `brightness-30` which wasn't a valid Tailwind step).
- Layout heading: `font-agincourt text-rbyellow-500 text-2xl sm:text-4xl md:text-feature-title`

## Page Widths

- Admin (new + edit): `max-w-2xl` (already in place)
- Release detail: added `max-w-2xl` (now matches admin)
- Browse: added `max-w-xl` (576px — slightly narrower)
- Home: unchanged at layout `max-w-5xl`

## Architecture Notes (TODO — not implemented)

- Track should become a top-level Firestore entity with `paths[]` (multiple GCS versions per track), `status`, `docLinks`
- Collections (currently "releases") will reference `{ trackId, gcsPath }` pairs
- Notes ownership model: version-level / collection-level / track-level
- Catalog stays as a dumb GCS sync index separate from the track entity
