# Session — 2026-07-13

## Done

- Fixed `.github/workflows/deploy.yaml`: removed a duplicated/stale step block, closed an unterminated quote on `DOCKER_BASE`, fixed `DOCKER_TAG` propagation (was set as a shell var then referenced as `${{ DOCKER_TAG }}`, which can never resolve — now written to `$GITHUB_ENV` and read back via `env.DOCKER_TAG`), unquoted `env_vars` values. Committed as `517d5bf`.
- Fixed `Dockerfile`: the `deps` stage copied `package.json` + `pnpm-lock.yaml` but not `pnpm-workspace.yaml`, so `pnpm install --frozen-lockfile` couldn't see the `overrides.gaxios` config and rejected the lockfile (`ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`). Added `pnpm-workspace.yaml` to the `COPY` line. Verified locally with `docker build --target deps`. Committed as `3cf9f77`.
- Regenerated `pnpm-lock.yaml` (came back byte-identical — it wasn't actually stale; the leftover `typescript@5.9.3` entry is isolated inside `vercel`'s own bundled toolchain, not a real conflict with the app's `typescript ^6.0.3` pin).
- Deleted `cloudbuild.yaml` (retired in favor of GitHub Actions), Node base image bumped to `node:24-alpine` (Node 20 deprecated).
- Repo secrets/vars confirmed set on `captainjspace/bandmagic`: secrets `PROJECT_ID`, `WIP`; vars `REGION=us-central1`, `ARTIFACT_REPO=band-magic-apps`, `SERVICE_NAME=band-magic`, `IMAGE_NAME=band-magic`, `DOCKER_BASE=us-central1-docker.pkg.dev/rollingblackoutapp`.
- Diagnosed and fixed `WIP` secret value — was pointing at a malformed/wrong provider. Correct value: `projects/901961412597/locations/global/workloadIdentityPools/githubpool/providers/github-provider` (there are **two** providers under pool `githubpool`: `github` is legacy/single-branch, `github-provider` is the Terraform-managed one with the multi-repo allowlist — always use `github-provider`).
- Design decision: using **direct principalSet IAM bindings** (not service-account impersonation) per user preference, matching the existing `captainjspace/resume` pattern. Reverted an earlier `service_account:` addition to the auth step accordingly.
- Confirmed via `gcloud` (read-only checks) that Docker build + push now succeeds end-to-end once `roles/artifactregistry.writer` was bound to the `bandmagic` principalSet on the `band-magic-apps` Artifact Registry repo.

## In Progress

- Deploy pipeline gets all the way to the `Deploy to Cloud Run` step and fails there with `PERMISSION_DENIED: iam.serviceaccounts.actAs` on `band-magic-runner@rollingblackoutapp.iam.gserviceaccount.com`.
- User has been applying IAM via Terraform, hit heavy friction (a `terraform apply` hung on a domain-mapping resource with no running Cloud Run revisions to attach to, had to kill it mid-apply, state is now inconsistent). User is pausing to purge/resync Terraform state before continuing.
- A tag `v1.0.4` already exists and triggered its own run (`29285884409`) — same result (build/push succeed, deploy step fails on the same `actAs` gap). No successful deploy yet.

## Blockers

- **Terraform state is out of sync** — user is purging and resyncing on their own before any further IAM changes. This session should not touch Terraform-managed GCP IAM further until that's done.
- My local `gcloud` identity (`joshgcp@rollingblackoutband.com`) lacks `setIamPolicy` on both service accounts and (likely) project-level IAM — I can read IAM state but cannot apply bindings myself. All grants so far were applied by the user via Terraform (or the GCP console).

## Next

1. Once Terraform is resynced, verify (read-only `gcloud` checks are safe and already scripted in this session's history) that these three bindings for the `bandmagic` principalSet (`principalSet://iam.googleapis.com/projects/901961412597/locations/global/workloadIdentityPools/githubpool/attribute.repository/captainjspace/bandmagic`) all survived:
   - `roles/run.admin` — project-level on `rollingblackoutapp`
   - `roles/artifactregistry.writer` — scoped to the `band-magic-apps` Artifact Registry repo (`us-central1`)
   - `roles/iam.serviceAccountUser` — on `band-magic-runner@rollingblackoutapp.iam.gserviceaccount.com` (**this one is still missing** — was the active blocker when the session paused)
2. Re-run the latest workflow run (or push a new commit) once the above is confirmed. Use `gh run view <id> --repo captainjspace/bandmagic --log-failed` to check; note that GitHub's "re-run failed jobs" replays the *original* commit SHA for that run — re-running an old run ID will NOT pick up newer commits, easy to get confused by (happened twice this session).
3. Untracked files sitting in the repo root, deliberately left alone this session — flag to user before they get swept into a future `git add -A`:
   - `runner` — a compiled Mach-O ARM64 binary. Not covered by `.gitignore` (which only excludes `runner/*`, `runner-decoder`, `runner.zsh`, `runner.sh` — not the bare `runner` file). User separately committed a *different* `runner` binary in `b09d434` ("relocated c hash of env") — worth confirming that was intentional.
   - `example.yaml` — reference file from another project, not meant for this repo.
4. Known bug documented in `CLAUDE.md` but not yet fixed: `src/lib/notify.ts` line 58 has `${siteConfig.url}}` (extra `}`) in the Chat notification template.
